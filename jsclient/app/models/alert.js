"use strict";


// Alert Model
// --------------
App.Models.Alert = Backbone.Model.extend({

    defaults: {
        "title": "",
        "message": ""
    }

    // This may get extended later but for now it's as simple as can be

});
